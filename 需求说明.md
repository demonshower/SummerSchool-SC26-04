# 出差行程规划助手 — 需求设计

## 1. 产品概述

出差行程规划助手面向商务差旅场景，提供行程创建、日程自动编排、多平台比价和攻略导入等核心能力。系统采用前后端分离架构（NestJS + Next.js），支持注册登录与游客体验两种模式。

---

## 2. 用户角色

| 角色 | 说明 |
|------|------|
| 游客 | 一键登录，系统自动创建临时账户，体验完整功能 |
| 注册用户 | 用户名+密码注册登录，持久化保存全部历史行程 |

---

## 3. 需求分类

### 3.1 行程规划

> 围绕出差行程的全生命周期：创建、日程编排、自动规划、活动项管理、费用与预算控制。

**3.1.1 行程创建与管理**

用户可以创建并维护出差行程，包括城市、日期、预算和会议安排。

- 创建行程：指定出发城市、目的城市、起止日期、最早出发/最晚返回时间、总预算、酒店预算上限
- 行程列表：分页查看所有行程，可按状态（草稿/规划中/就绪/已确认/归档）筛选
- 行程详情：查看完整行程信息，包括会议、每日活动项、费用汇总、规划版本
- 编辑行程：修改预算、偏好等设置，采用版本号防止并发覆盖
- 删除行程：级联删除会议、天数、活动项等全部关联数据

*对应代码：`backend/src/modules/trips/trips.service.ts`*

**3.1.2 会议管理**

用户可以在创建行程时添加会议安排，会议信息参与后续日程编排。

- 新增会议：设定标题、日期、起止时间（HH:mm）、地点、经纬度、备注
- 会议与日程联动：规划引擎根据会议时间自动计算每天的可用空闲时段

*对应代码：`backend/src/modules/trips/dto/trip.dto.ts`（CreateMeetingDto）、`backend/src/modules/planning/planning.service.ts`（calculateFreeSlots）*

**3.1.3 活动项管理**

用户可以对已规划的每日日程进行增删改和排序调整。

- 8 种活动类型：交通、市内交通、住宿、餐饮、景点、会议、固定事项、空闲
- 活动项操作：新增、编辑（标题/时间/地点/费用/备注）、删除、移动至其他日期
- 锁定机制：系统生成的关键日程可锁定，锁定后禁止删除和编辑
- 批量排序：一次性调整某天所有活动项的排列位置
- 日费用同步：活动项变更后自动重新计算该日总费用

*对应代码：`backend/src/modules/itinerary/itinerary.service.ts`、`backend/src/modules/itinerary/dto/itinerary.dto.ts`*

**3.1.4 自动规划引擎**

系统根据行程参数和策略自动生成完整的每日日程安排。

- 日期分析：自动判定每天类型（出发日/会议日/返回日/自由日）
- 空闲时段计算：根据当天的会议时间计算出可用于安排活动的时段
- 自动生成交通项：出发/返程的火车或飞机行程
- 自动生成住宿项：每晚从目的地城市匹配酒店
- 自动生成活动项：根据策略（经济/均衡/舒适）从 Place 表选取景点和餐饮填入空闲时段

*对应代码：`backend/src/modules/planning/planning.service.ts`*

**3.1.5 费用汇总与预算对比**

系统自动汇总全程各类费用并与用户预算进行对比。

- 按类型分类汇总：交通费、住宿费、餐饮费、市内交通费、景点门票费
- 预算状态判定：未超额 / 接近预算（超支 <10%）/ 略超（超支 <30%）/ 严重超支
- 每日费用自动计算：基于当天活动项的 costMinor 累加

*对应代码：`backend/src/modules/trips/trips.service.ts`（calculateCostSummary）、`backend/src/modules/itinerary/itinerary.service.ts`（updateDayCost）*

---

### 3.2 旅游规划

> 围绕目的地的旅游信息获取：地点搜索、多平台报价比价、攻略内容导入与地点抽取。

**3.2.1 地点与城市信息**

用户可以在系统中搜索和浏览目的地相关的地点信息。

- 城市列表：返回系统支持的全部城市（当前 15 个，含名称、省份、经纬度、时区）
- 地点搜索：按城市、关键词、类别（景点/餐厅/酒店/交通站/购物）搜索 POI
- 地点详情：查看单个地点的名称、地址、经纬度、分类、扩展属性

*对应代码：`backend/src/modules/places/places.service.ts`、`backend/prisma/seed.ts`（15 城市 + 40 POI 种子数据）*

**3.2.2 报价比价**

用户可按产品类型搜索多平台报价并进行横向对比。

- 搜索报价：按产品类型（火车/飞机/酒店/门票）触发报价搜索
- 报价分组对比：同一产品不同平台的报价按 comparableKey 分组展示
- 报价刷新：刷新单个报价的最新价格和库存状态
- 库存状态标识：可预订 / 库存紧张 / 已售罄 / 未知
- 深链跳转：生成供应商预订链接，用户可跳转至外部平台预订

*对应代码：`backend/src/modules/quotes/quotes.service.ts`、`frontend/src/components/quotes/QuotePanel.tsx`*

**3.2.3 攻略导入与地点抽取**

用户可以导入游记或攻略文本，系统自动识别其中的旅游地点。

- 文本导入：粘贴攻略内容，系统自动识别地名并生成抽取结果
- 链接导入：输入攻略 URL，系统保存链接元数据
- 地点抽取：每个识别结果包含地名、建议游玩时长、推荐时段（上午/下午/晚上）
- 情感标注：标注攻略中对地点的态度（推荐 / 避坑 / 中性），附置信度评分
- 地点匹配：将识别的名称与系统 Place 表已有 POI 进行关联

*对应代码：`backend/src/modules/content/content.service.ts`、`frontend/src/components/content/GuideImporter.tsx`*

---

### 3.3 个性化风格

> 围绕用户偏好定制、认证安全与系统体验。

**3.3.1 用户认证**

系统支持注册登录和游客两种使用方式。

- 注册：用户名（唯一，≤80字符）+ 密码（≥6位，bcrypt 加密）+ 可选邮箱
- 登录：用户名 + 密码，返回 JWT Token（7天有效）
- 游客模式：一键登录，系统自动创建 `guest_` 前缀的临时账户
- 401 拦截：Token 过期或无效时前端自动清除状态并跳转登录页

*对应代码：`backend/src/modules/auth/auth.service.ts`、`frontend/src/lib/api/client.ts`、`frontend/src/components/auth/AuthGuard.tsx`*

**3.3.2 出行偏好设置**

用户在创建行程时可设置多种偏好，这些偏好影响后续的自动规划结果。

- 交通偏好：不限 / 火车优先 / 飞机优先
- 酒店预算上限：设定每晚住宿的最高金额（以分为单位）
- 观光偏好：不限 / 自然风光 / 人文历史 / 美食 / 购物
- 行程节奏：轻松 / 均衡 / 紧凑
- 规划策略：经济型（低价优先）/ 均衡型（默认）/ 舒适型（高品质优先）

*对应代码：`backend/src/modules/trips/dto/trip.dto.ts`（CreateTripDto）、`backend/prisma/schema.prisma`（Trip 模型字段）*

**3.3.3 版本控制与数据安全**

系统在编辑操作中引入并发控制机制，确保多人/多端操作数据一致。

- 乐观锁：Trip 和 ItineraryItem 的编辑操作均校验 version 字段，冲突时返回版本冲突错误
- 软删除：用户账户支持软删除，保留数据可追溯
- 审计字段：所有表含 createdAt、updatedAt 时间戳
- 操作日志：UserEvent 表记录用户的关键操作行为

*对应代码：`backend/src/common/constants/error-codes.ts`（TRIP_VERSION_CONFLICT）、`backend/prisma/schema.prisma`（User.deletedAt、UserEvent）*

---

## 4. 非功能需求

**性能**
- 普通查询接口响应时间 ≤ 500ms
- 支持 50 并发用户

**安全**
- 密码 bcrypt 不可逆加密
- JWT 认证，接口级资源所有权校验
- DTO 白名单校验，拒绝非预期字段
- CORS 白名单限制

**可靠性**
- 编辑操作乐观锁防并发覆盖
- 软删除保护数据
- 规划失败自动回滚至草稿状态

**可维护性**
- Swagger 自动化 API 文档
- 22 个统一错误码，前后端共用
- 全栈 TypeScript
- Prisma Migrate 管理数据库变更
