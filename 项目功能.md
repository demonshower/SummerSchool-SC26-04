# 出差行程规划助手 — 项目功能

## 1. 项目概述

**项目名称**：出差行程规划助手 (Trip Planner)

**项目描述**：面向商务差旅用户的行程规划 Web 应用，支持创建行程、自动规划日程、报价比价和攻略导入等功能。采用前后端分离架构。

**版本**：V1.0

---

## 2. 技术栈

### 2.1 后端

| 类别 | 技术 |
|------|------|
| 运行时 | Node.js v22+ |
| 框架 | NestJS 10 |
| 语言 | TypeScript 5 |
| ORM | Prisma 5 |
| 数据库 | PostgreSQL |
| 缓存 | Redis (ioredis) |
| 认证 | JWT (passport-jwt) + bcrypt |
| 校验 | class-validator / class-transformer |
| API 文档 | Swagger (nestjs/swagger) |
| 测试 | Vitest |

### 2.2 前端

| 类别 | 技术 |
|------|------|
| 框架 | Next.js 16 (App Router) |
| 语言 | TypeScript |
| UI | Bootstrap 5.3 + Bootstrap Icons |
| 数据获取 | TanStack Query v5 |
| 表单 | React Hook Form + Zod |
| 状态管理 | Zustand |
| HTTP | Axios |
| 日期 | date-fns |

### 2.3 部署

Docker / Docker Compose 容器化，Nginx 反向代理，pnpm (后端) / npm (前端)

---

## 3. 项目结构

```
SummerSchool-SC26-04/
├── backend/
│   ├── src/
│   │   ├── main.ts                     # 入口：CORS、ValidationPipe、Swagger
│   │   ├── app.module.ts               # 根模块
│   │   ├── common/                     # 公共层：错误码、异常、过滤器、拦截器、装饰器
│   │   ├── prisma/                     # PrismaService 全局数据库连接
│   │   └── modules/
│   │       ├── auth/                   # 认证模块
│   │       ├── users/                  # 用户模块
│   │       ├── trips/                  # 行程 CRUD + 费用汇总
│   │       ├── itinerary/              # 活动项增删改/移动/锁定/排序
│   │       ├── planning/               # 规则引擎自动规划
│   │       ├── quotes/                 # 报价比价 (Mock)
│   │       ├── content/                # 攻略导入与地点抽取
│   │       └── places/                 # 地点/城市查询
│   ├── prisma/
│   │   ├── schema.prisma               # 14 张表
│   │   └── seed.ts                     # 15 城市 + 40 POI
│   └── doc/
│       ├── deployment.md               # 部署指南
│       └── api-v1.md                   # API 文档 (33 个接口)
│
├── frontend/
│   └── src/
│       ├── app/                        # 7 个页面 (首页/登录/注册/列表/新建/详情/编辑)
│       ├── components/                 # 组件 (Navbar/AuthGuard/ItemEditor/QuotePanel/GuideImporter)
│       ├── lib/api/                    # 8 个 API 模块
│       ├── lib/utils/                  # money.ts / date.ts
│       ├── stores/                     # authStore (Zustand)
│       ├── types/                      # TypeScript 类型定义
│       └── providers/                  # TanStack Query Provider
│
├── doc/                                # 项目文档
└── README.md
```

---

## 4. 启动方式

### 后端

```bash
cd backend
cp .env.example .env
npx pnpm@9 install --frozen-lockfile
npx pnpm@9 prisma:generate
npx pnpm@9 start:dev
# → http://localhost:8080
# Swagger → http://localhost:8080/api/docs
```

### 前端

```bash
cd frontend
npm ci
echo 'NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1' > .env.local
npm run dev
# → http://localhost:3000
```

### Docker

```bash
cd backend && docker compose up -d
```

---

## 5. 功能分类

### 5.1 行程规划

> 行程的全生命周期管理与日程自动编排。

**5.1.1 行程管理**

以行程为聚合根，管理出差全过程的基础信息和会议安排。

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 创建行程 | 城市/日期/预算/酒店上限/偏好/会议一并提交，会议含标题/时间/地点/经纬度 | `trips/trips.service.ts`、`trips/dto/trip.dto.ts` |
| 行程列表 | 分页查询 + status 筛选 (draft/planning/ready/confirmed/archived)，含 meeting 数量和 day 数量 | `trips/trips.service.ts`（findAll） |
| 行程详情 | 加载 meetings、days、items、quoteSearches、planVersions，含完整费用汇总 | `trips/trips.service.ts`（findOne） |
| 编辑行程 | Partch 更新偏好、预算等字段，携带 version 做乐观锁校验，更新后 version+1 | `trips/trips.service.ts`（update） |
| 删除行程 | 验证所有权，级联删除全部子表数据（ON DELETE CASCADE） | `trips/trips.service.ts`（remove） |

**5.1.2 活动项管理**

对每日日程中的活动项进行增删改和排序操作。

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 查看按天日程 | 查询 trip 下所有 TripDay，items 按 position 排序返回 8 种 kind | `itinerary/itinerary.service.ts`（getDaysWithItems） |
| 新增活动项 | 指定 dayId + kind，未传 position 时自动追加末尾，创建后更新 day 总费用 | `itinerary/itinerary.service.ts`（createItem） |
| 编辑活动项 | 校验非 locked 状态，携带 version 乐观锁更新，同步更新 day 费用 | `itinerary/itinerary.service.ts`（updateItem） |
| 删除活动项 | locked 或 isFixed 项不可删除，删除后重算 day 费用 | `itinerary/itinerary.service.ts`（deleteItem） |
| 移动活动项 | 变更 dayId 或 position，校验目标 day 存在 + trip version 冲突 | `itinerary/itinerary.service.ts`（moveItem） |
| 锁定/解锁 | 设置 locked 标志，防止误操作系统生成的关键日程 | `itinerary/itinerary.service.ts`（lockItem） |
| 批量排序 | 传入 itemId → position 映射数组，事务一次性批量更新 | `itinerary/itinerary.service.ts`（reorderItems） |

**5.1.3 自动规划引擎**

基于规则的引擎，根据出行参数和策略自动生成完整日程。

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 触发规划 | 接收 strategy (budget/balanced/comfort)，trip 状态 → planning → ready | `planning/planning.service.ts`（createPlan） |
| 日期分析 | 逐天判定 dayType：第一天→departure，最后一天→return，有会议→meeting_day，其余→free | `planning/planning.service.ts`（analyzeDays） |
| 空闲计算 | 全天窗口 [08:00, 22:00] 减去会议占用的时段，按 dayType 调整起止 | `planning/planning.service.ts`（calculateFreeSlots） |
| 交通生成 | 出发/返程日生成 train 或 flight 类型 transport 项 | `planning/planning.service.ts`（generateTransport） |
| 酒店生成 | 每晚生成 hotel 项，从 Place 表按城市名 + category=hotel 查询 | `planning/planning.service.ts`（generateHotels） |
| 活动生成 | 按 strategy 和 attractionPreference 评分选取景点，空闲时段插入景点+餐饮 | `planning/planning.service.ts`（generateActivities） |
| 版本记录 | PlanVersion 表记录版本号 + 策略，规划失败回滚 trip 状态至 draft | `planning/planning.service.ts`、`prisma/schema.prisma` |

**5.1.4 费用汇总与预算对比**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 分类汇总 | 遍历所有 day.items，按 kind 累加 → transport/hotel/meal/commute/attraction 五类 | `trips/trips.service.ts`（calculateCostSummary） |
| 预算状态 | ratio = (budget - total) / budget；≥0→within_budget，<0 且 <10%→close_to_budget，<30%→slightly_over，≥30%→over_budget | `trips/trips.service.ts` |
| 日费用同步 | day 内 item 变更后查询 SUM(costMinor) 更新 TripDay.totalCostMinor | `itinerary/itinerary.service.ts`（updateDayCost） |

---

### 5.2 旅游规划

> 目的地旅游信息获取与辅助决策：地点查询、多平台报价比价、攻略内容导入。

**5.2.1 地点与城市查询**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 城市列表 | 返回 cities 表全部活跃城市（15 个），含 code/name/province/lat/lng | `places/places.service.ts`、`prisma/seed.ts` |
| 地点搜索 | 按 cityCode + keyword + category 组合过滤 places 表，模糊匹配 canonicalName | `places/places.service.ts` |
| 地点详情 | 按 id 查询单个 Place，含 attributes JSON 扩展字段 | `places/places.service.ts` |
| 种子数据 | 15 个城市（沪深北杭南苏昆成西广深渝武长厦青）+ 40 个 POI（含景点/餐厅/酒店/车站/机场） | `prisma/seed.ts` |

**5.2.2 报价比价**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 搜索报价 | 指定 productType (train/flight/hotel/ticket)，生成 SHA256 requestHash 做请求去重缓存 | `quotes/quotes.service.ts`（searchQuotes） |
| 报价列表 | 查询 QuoteSearch → Quote，支持按 productType 过滤 | `quotes/quotes.service.ts` |
| 前端分组对比 | QuotePanel 按 comparableKey 分组，同组多平台横向比较，显示 providerCode/价格/库存/退改/过期 | `frontend/src/components/quotes/QuotePanel.tsx` |
| 刷新报价 | 单个 Quote 的 observedAt / price / inventoryStatus 更新 | `quotes/quotes.service.ts`（refreshQuote） |
| 深链跳转 | 生成 supplier deepLink，前端打开新窗口跳转外部预订页 | `quotes/quotes.service.ts`（generateClickout） |
| 库存状态 | available / limited / sold_out / unknown 四种状态，前端以颜色标记 | `frontend/src/components/quotes/QuotePanel.tsx` |

**5.2.3 攻略导入与抽取**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 文本导入 | ContentSource(sourceType=text) + 解析 rawText 抽取地名 | `content/content.service.ts`（importText） |
| 链接导入 | ContentSource(sourceType=link) + 保存 sourceUrl 元数据 | `content/content.service.ts`（importLink） |
| 地点抽取结果 | PlaceMention 含 mentionText / suggestedDurationMin / suggestedPeriod / sentiment / confidence / tips | `content/content.service.ts` |
| 情感标注 | positive(推荐) / negative(避坑) / neutral(中性)，附 0-1 置信度 | `content/content.service.ts` |
| 地点匹配 | mentionText 匹配 Place 表 canonicalName，自动关联 placeId | `content/content.service.ts` |
| 前端交互 | GuideImporter 支持文本/链接 Tab 切换，结果卡片展示地名+置信度+情感+时长+时段+Tips | `frontend/src/components/content/GuideImporter.tsx` |

---

### 5.3 个性化风格

> 用户认证、出行偏好设置、系统安全与数据一致性保障。

**5.3.1 用户认证**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 注册 | username (唯一, ≤80) + password (≥6, bcrypt cost=10) + email (可选) | `auth/auth.service.ts`（register） |
| 登录 | username + password → bcrypt.compare → JWT sign {sub, isGuest}，7天有效 | `auth/auth.service.ts`（login） |
| 游客模式 | 自动创建 guest_ 前缀用户，isGuest=true，无密码直接返回 token | `auth/auth.service.ts`（guestLogin） |
| JWT 策略 | ExtractJwt.fromAuthHeaderAsBearerToken，校验签名+过期，数据库加载最新用户信息 | `auth/strategies/jwt.strategy.ts` |
| 前端认证流 | Zustand persist Token 至 localStorage，Axios 拦截器附加 Header，401 清除状态跳登录 | `frontend/src/stores/authStore.ts`、`frontend/src/lib/api/client.ts`、`frontend/src/components/auth/AuthGuard.tsx` |

**5.3.2 出行偏好**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 交通偏好 | any / train / flight，影响规划引擎的交通生成逻辑 | `trips/dto/trip.dto.ts`（CreateTripDto.transportPreference） |
| 酒店预算 | hotelMaxPriceMinor（分），规划引擎匹配 Place 表酒店时按价格过滤 | `prisma/schema.prisma`（Trip.hotelMaxPriceMinor） |
| 观光偏好 | any / nature / culture / food / shopping，影响活动生成中的景点筛选 | `trips/dto/trip.dto.ts`（CreateTripDto.attractionPreference） |
| 行程节奏 | relaxed / balanced / intense，影响每天活动数量和间隔 | `prisma/schema.prisma`（Trip.pace） |
| 规划策略 | budget / balanced / comfort，控制酒店档位/餐厅档次/景点选择 | `planning/dto/planning.dto.ts` + `planning/planning.service.ts` |
| 语言偏好 | locale 字段（默认 zh-CN），支持用户修改 | `users/dto/user.dto.ts`（UpdateUserDto.locale） |

**5.3.3 安全与可靠性**

| 功能 | 实现要点 | 代码位置 |
|------|---------|---------|
| 资源所有权 | 所有业务接口通过 CurrentUser 装饰器获取 userId，校验 ownerId === userId | `trips/trips.service.ts`（verifyOwnership）、`common/decorators/current-user.decorator.ts` |
| 乐观锁 | Trip.version / ItineraryItem.version 做版本对比，冲突返回 TRIP_VERSION_CONFLICT | `trips/trips.service.ts`、`itinerary/itinerary.service.ts` |
| 软删除 | User.deletedAt 字段，查询时过滤 deletedAt IS NOT NULL 的记录 | `prisma/schema.prisma`（User.deletedAt）、`users/users.service.ts` |
| 操作日志 | UserEvent 表记录 userId + eventType + entityType + entityId + context JSON + occurredAt | `prisma/schema.prisma`（UserEvent） |
| 全局异常处理 | AllExceptionsFilter 捕获全部异常，统一输出 {error: {code, message, details, path, timestamp}} | `common/filters/all-exceptions.filter.ts` |
| 响应包装 | TransformInterceptor 统一包裹 {data, meta: {requestId, timestamp}} | `common/interceptors/transform.interceptor.ts` |
| 输入校验 | ValidationPipe(whitelist: true, forbidNonWhitelisted: true) + DTO class-validator 装饰器 | `main.ts`、各模块 dto 文件 |
| 规划回滚 | createPlan 中 try-catch，出错时 trip.status → draft | `planning/planning.service.ts`（createPlan） |

---

## 6. 数据库

PostgreSQL，14 张表，Prisma ORM 管理。

| 表 | 用途 |
|------|------|
| users | 用户账户（含 isGuest、软删除、locale） |
| trips | 行程聚合根（含所有偏好字段 + 版本号） |
| trip_meetings | 行程关联的会议 |
| trip_days | 每日分组（dayType、预算、费用） |
| itinerary_items | 活动项（8 种 kind、position、locked、版本号） |
| transport_segments | 交通段的详细信息（站名、承运商、时长、距离） |
| places | POI 地点库（canonicalName、category、cityCode、经纬度、attributes） |
| cities | 支持的城市列表（code、name、province、经纬度） |
| quote_searches | 报价搜索记录（hash 去重、criteria JSON） |
| quotes | 报价快照（append-only，含价格/库存/退改/过期/深链） |
| content_sources | 攻略内容源（文本/链接，含 rawText、summary） |
| place_mentions | 抽取的地点提及（置信度、情感、时长、小贴士、与 Place 关联） |
| plan_versions | 规划版本（版本号、策略、状态） |
| user_events | 用户操作日志 |

种子数据：15 城市 + 40 POI（`prisma/seed.ts`）

---

## 7. API 设计约定

### 响应格式

```json
{
  "data": { ... },
  "meta": { "requestId": "req_xxx", "timestamp": "2026-07-18T12:00:00.000Z" }
}
```

### 错误格式

```json
{
  "error": {
    "code": "TRIP_NOT_FOUND",
    "message": "行程不存在",
    "details": {},
    "path": "/api/v1/trips/xxx"
  }
}
```

### 约定

- **金额**：API 用分（`xxxMinor`），前端用元，`fenToYuan` / `yuanToFen` 转换
- **时间**：ISO 8601，数据库 UTC，日期用 `@db.Date`，时刻用 `HH:mm`
- **ID**：全局 CUID
- **API 前缀**：`/api/v1`

---

## 8. 认证流程

```
注册/登录 → JWT Token (7天) → Zustand 持久化 to localStorage
       → Axios 拦截器自动附加 Authorization: Bearer <token>
       → 401 → 清除状态 → 跳转 /auth/login
```

游客模式：一键创建 `guest_` 前缀临时账户，`isGuest=true`，无需密码。
